import { EmailManager as EmailManagerComponent } from "@/components/Admin/EmailManager/EmailManager";

const EmailManager = () => {
  return <EmailManagerComponent />;
};

export default EmailManager;