export interface UnifiedMedia {
  id: string;
  url: string;
  thumbnail_url?: string;
  source_table: string;
  type: string;
  title?: string;
  description?: string;
  category: string;
  created_at?: string;
}